---
timezone: UTC+8
---

# G-H11

**GitHub ID:** G-H11

**Telegram:** 

## Self-introduction

Web3 暑期实习计划 - Monad Buidler Camp

## Notes

<!-- Content_START -->
# 2026-07-09
<!-- DAILY_CHECKIN_2026-07-09_START -->
# 为什么Monad体验不同

### 方向选择：链上订单簿交易所（On-chain CLOB）

### 一、Research：为什么 Monad 更适合这个高频交互应用

1\. 产品场景为何天然需要“高频”交互？

在订单簿模型中，市场由**做市商（Maker）和吃单者（Taker）共同驱动。做市商的核心职责不是“持有资产”，而是通过连续挂单和撤单来赚取买卖价差**。这是一场围绕“队列优先级”的毫秒级竞赛：

-   **价格发现的连续性**：在中心化交易所（CEX）中，顶级做市商的订单更新频率通常在**每秒 50~100 次**以上。每当外部市场价格发生 0.01% 的波动，链上做市商必须立即撤掉旧单并挂出新单，否则其挂单会被套利者迅速吃掉。
    
-   **订单与交易比极高**：传统金融中，挂单/撤单次数与实际成交次数的比值常高达**100:1**。这意味着一个每日成交 1 万笔的活跃交易对，背后隐藏着**每日超过 100 万次的链上状态写入**。如此高频的状态变更，决定了 CLOB 是区块链性能的“极限测试场”。
    

2\. 拥堵链和高 Gas 费带来的“死亡螺旋”困境

在 Solidity 生态（以太坊主网或低性能 L2）上尝试 CLOB 会陷入三重困境，这也解释了为何过去 DeFi 只能退而求其次选择 AMM：

-   **延迟导致的滑点灾难**：以太坊 12 秒的出块时间意味着，当用户提交限价单时，指令可能在多个区块后才被确认，此时市场价格早已变化，订单要么无法成交要么以不利价格成交。对于做市商，这种延迟等同于“蒙眼接飞刀”。
    
-   **Gas 费吞噬做市利润**：在以太坊网络拥堵时，一笔简单的 `cancelOrder` 操作可能耗费数十美元 Gas 费。假设一个做市商每天需要调整 1,000 次订单，其单日运营成本就高达数万美元。这直接导致链上 CLOB 的买卖点差必须极大才能覆盖成本，流动性陷入“越差越薄，越薄越差”的负循环。
    

3\. Monad 如何从底层架构颠覆这一痛点？

Monad 的并行执行架构和超高 TPS 不仅带来了“数字上的快”，更重要的是带来了**经济可行性与策略精确性**的质变。

Monad 是一个高性能的 EVM 兼容 Layer 1 区块链，**于 2025 年 11 月 24 日正式上线主网**。其核心性能指标包括：

-   **每秒 10,000 笔交易（10,000 TPS）** 的吞吐量
    
-   **0.4 秒出块时间**
    
-   **约 0.8 秒的交易最终确认（finality）**
    

Monad 由前 Jump Trading 高频交易团队成员创立，将高频交易的优化原则直接应用到了区块链架构中。其核心技术包括**乐观并行执行（Optimistic Parallel Execution）** ——独立交易可并行处理，仅在发生状态冲突时回退到顺序执行——以及**MonadBFT 流水线共识机制**。

这让**完全链上的 CLOB 在 EVM 生态中首次变得可行**：

-   **0.8 秒终局性与 0.4 秒区块时间**：做市商可以在**1 秒内完成撤单+重挂**，将价格风险敞口压缩到极低水平。
    
-   **极低的交易成本**：Monad 的高吞吐量使得单笔挂撤单的 Gas 费维持在美分级甚至更低。做市商可以频繁更新报价而不会产生显著成本负担。
    
-   **EVM 完全兼容，无需改代码**：Monad 100% 兼容 EVM 字节码，开发者可以使用熟悉的 Solidity、Hardhat、Foundry 等工具直接部署。现有的 DeFi 做市商和 MEV 机器人无需学习新语言即可迁移策略。
    

4\. 为什么“普通数据库”无法替代？

很多人会问：既然订单簿需要高频更新，为何不采用“链下撮合 + 链上结算”的混合模式？这种模式虽然可行，但牺牲了 DeFi 最核心的 **“可组合性”与“透明度”**：

-   **原子性与可组合性**：在纯链上 CLOB 中，**“订单撮合”与“资产交割”是一个不可分割的原子事务**。用户可以将 CLOB 中的限价单直接作为借贷清算的触发条件，或与其他 DeFi 协议组合成复杂策略。这是任何中心化数据库永远无法提供的终极护城河。
    
-   **抗审查与可验证性**：传统数据库由项目方中心化控制，存在“插队”或定向撤单的风险。而 Monad 上的 CLOB 将撮合逻辑运行在开放的虚拟机中，任何第三方节点都可以独立验证每一笔挂单是否按照规则公平执行。
    

Monad 生态中已涌现出一批原生 CLOB 项目。**Kuru** 是 Monad 生态中首个完全链上的中央限价订单簿（CLOB）DEX，已获得 Electric Capital 领投的 200 万美元种子轮融资，目前已完成 1000 万美元融资并在 X 上新增 12 万粉丝。**Clober** 是 Monad 上的链上订单簿基础设施协议，引入了 LOBSTER（Limit Order Book with Segment Tree for Efficient oRder-matching）机制来优化 Gas 效率。**DrakeExchange** 则是完全链上的永续合约 DEX，采用 CLOB+vAMM 混合模式。这些项目的集中涌现，本身就说明了 Monad 在 CLOB 方向上的独特吸引力。

### 二、Tech：高频交互 Demo 功能清单

以下是一个 Monad 链上 CLOB 交易应用的核心功能模块（可参考 Kuru、Clober 等 Monad 原生 CLOB 项目的设计思路）：

**1\. 订单管理模块**

-   **限价单挂单**：用户指定价格和数量，订单实时写入链上订单簿
    
-   **限价单撤单**：做市商可高频取消未成交订单，Gas 成本极低
    
-   **订单修改**：支持修改价格或数量，而非先撤后挂（减少链上操作次数）
    
-   **市价单**：按当前最优价格立即成交
    

**2\. 订单簿实时同步**

-   利用 Monad 的 0.4 秒出块和 0.8 秒终局性，前端实时渲染买卖盘口深度
    
-   交易历史推送延迟控制在毫秒级
    
-   支持 WebSocket 订阅实时订单簿更新
    

**3\. 撮合与结算**

-   链上智能合约自动撮合匹配的买卖单
    
-   资产原子性结算（买方 Token 减少、卖方 Token 增加同步完成）
    
-   部分成交支持（订单可被多笔对手单分批吃掉）
    
-   支持永续合约（perps）和现货交易
    

**4\. 做市商工具**

-   多订单批量提交（单笔交易包含多个挂单/撤单操作）
    
-   价格跟随策略（监听链上价格变化，自动调整挂单）
    
-   Gas 预估与优化建议
    
-   利用 Monad 并行执行能力支持复杂做市策略
    

**5\. 用户面板**

-   实时持仓与盈亏展示
    
-   历史成交记录
    
-   深度图与 K 线（数据来自链上索引服务）
    
-   支持 MetaMask、Phantom、Rabby 等钱包
    

### 三、Ops：3 个适合 Meme 或社区传播的产品切入点

**切入点 1：“10000 TPS 挑战赛”**

发起社区挑战：在 Monad 主网上，谁能用我们的 CLOB 在 1 分钟内完成最多笔交易（挂单+撤单+成交），就能获得奖励。由于 Monad 支持每秒 10,000 笔交易，用户可以在 1 分钟内轻松完成数百次操作。将挑战过程录屏并配上夸张的“这真的是区块链？”标题在 Crypto Twitter 传播。天然适合制作对比型 Meme 内容。

**切入点 2：“Gas 费 0.001 美元挂单日”**

策划一个主题活动日：所有用户在 CLOB 上挂单，Gas 费由项目方补贴（实际上 Monad 本身的费用已经极低）。核心传播点是“在以太坊上挂一个单的 Gas 费，在 Monad 上可以挂 1000 次”。配合 UGC 内容——“我今天挂了 500 个订单，花了不到 1 美元”——这种反常识的体验会自然引发社区讨论和二次传播。Meme 角度：把以太坊 Gas 费比作“买一杯咖啡”，Monad 的 Gas 费比作“咖啡杯里的一粒糖”。Monad 主网已于 2025 年 11 月 24 日正式上线，已有 280+ 个项目在建，社区基础扎实。

**切入点 3：“社区做市商计划”**

让社区成员成为“迷你做市商”。提供简单的做市策略工具，任何用户都可以用少量资金在 CLOB 上挂单赚取买卖价差。在 Monad 的低延迟环境下，普通用户的挂单也能被及时成交。通过排行榜展示“今日最佳做市收益”，激发社区成员的参与感和竞争欲。Meme 方向：“以前做市商是华尔街精英的事，现在在 Monad 上，你也可以”——将高频交易“平民化”的叙事与 Monad 团队来自 Jump Trading 的高频交易背景相结合，形成“高性能属于每个人”的品牌认知。可参考 Kuru 的 Vaults 系统 + Meme 币交易竞赛模式来提升参与度。
<!-- DAILY_CHECKIN_2026-07-09_END -->

# 2026-07-08
<!-- DAILY_CHECKIN_2026-07-08_START -->

# **AI 辅助开发｜用 AI 生成一个最小 Solidity 合约**

### 1、我使用的 Prompt：

![image.png](attachment:119595cc-873e-4bc7-a507-bde855e50cef:image.png)

### 2、AI 生成的主要输出：

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

/**
 * @title 去中心化留言板（按地址存储）
 * @notice 每个地址可以发布留言，并可通过地址查询其所有留言
 */
contract Guestbook {
    // 留言结构体：内容 + 时间戳
    struct Message {
        string content;
        uint256 timestamp;
    }

    // 每个地址 => 该地址发布的所有留言数组
    mapping(address => Message[]) private userMessages;

    // 留言添加事件，便于前端监听
    event MessageAdded(
        address indexed user,
        uint256 indexed index,
        string content,
        uint256 timestamp
    );

    /**
     * @dev 发布新留言（仅限调用者本人）
     * @param _content 留言内容（非空，长度 <= 500 字符）
     */
    function addMessage(string memory _content) public {
        require(bytes(_content).length > 0, "Content cannot be empty");
        require(bytes(_content).length <= 500, "Content too long (max 500 chars)");

        uint256 index = userMessages[msg.sender].length;
        userMessages[msg.sender].push(Message({
            content: _content,
            timestamp: block.timestamp
        }));

        emit MessageAdded(msg.sender, index, _content, block.timestamp);
    }

    /**
     * @dev 查询某个地址的留言总数
     * @param _user 要查询的地址
     * @return 留言条数
     */
    function getMessageCount(address _user) public view returns (uint256) {
        return userMessages[_user].length;
    }

    /**
     * @dev 查询某个地址的指定索引的留言
     * @param _user 要查询的地址
     * @param _index 留言索引（从 0 开始）
     * @return content 留言内容
     * @return timestamp 发布时间戳
     */
    function getMessage(address _user, uint256 _index) public view returns (string memory content, uint256 timestamp) {
        require(_index < userMessages[_user].length, "Index out of bounds");
        Message memory msg = userMessages[_user][_index];
        return (msg.content, msg.timestamp);
    }

    /**
     * @dev 获取某个地址的所有留言（返回完整数组）
     * @notice 若留言过多可能触发 gas 限制，建议使用分页查询（可参考扩展说明）
     * @param _user 要查询的地址
     * @return 该地址的所有留言数组
     */
    function getAllMessages(address _user) public view returns (Message[] memory) {
        return userMessages[_user];
    }
}
```

### 3、修改后的源码：

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

/**
 * @title 去中心化留言板（按地址存储）
 * @notice 每个地址可以发布留言，并可通过地址查询其所有留言
 */
contract Guestbook {
    // 留言结构体：内容 + 时间戳
    struct Message {
        string content;
        uint256 timestamp;
    }

    // 每个地址 => 该地址发布的所有留言数组
    mapping(address => Message[]) private userMessages;

    // 留言添加事件，便于前端监听
    event MessageAdded(
        address indexed user,
        uint256 indexed index,
        string content,
        uint256 timestamp
    );

    /**
     * @dev 发布新留言（仅限调用者本人）
     * @param _content 留言内容（非空，长度 <= 500 字符）
     */
    function addMessage(string memory _content) public {
        require(bytes(_content).length > 0, "Content cannot be empty");
        require(bytes(_content).length <= 500, "Content too long (max 500 chars)");

        uint256 index = userMessages[msg.sender].length;
        userMessages[msg.sender].push(Message({
            content: _content,
            timestamp: block.timestamp
        }));

        emit MessageAdded(msg.sender, index, _content, block.timestamp);
    }

    /**
     * @dev 查询某个地址的留言总数
     * @param _user 要查询的地址
     * @return 留言条数
     */
    function getMessageCount(address _user) public view returns (uint256) {
        return userMessages[_user].length;
    }

    /**
     * @dev 查询某个地址的指定索引的留言
     * @param _user 要查询的地址
     * @param _index 留言索引（从 0 开始）
     * @return content 留言内容
     * @return timestamp 发布时间戳
     */
    function getMessage(address _user, uint256 _index) public view returns (string memory content, uint256 timestamp) {
        require(_index < userMessages[_user].length, "Index out of bounds");
        Message memory msgs = userMessages[_user][_index];
        return (msgs.content, msgs.timestamp);
    }

    /**
     * @dev 获取某个地址的所有留言（返回完整数组）
     * @notice 若留言过多可能触发 gas 限制，建议使用分页查询（可参考扩展说明）
     * @param _user 要查询的地址
     * @return 该地址的所有留言数组
     */
    function getAllMessages(address _user) public view returns (Message[] memory) {
        return userMessages[_user];
    }
}
```

### 4、修改内容：

修改前：

![image.png](attachment:a61b873c-1b80-402c-9b6a-6a915282f258:image.png)

修改后：

![image.png](attachment:2b45e38a-3ff3-41ae-a8d3-c2f56a43677e:image.png)

解释：修改前的变量msg是重写了solidity中内置的变量名，后做出人工修改。

### 5、检查点：

①合约是否能编译。

②函数是否符合你的预期。

③变量和函数命名是否容易理解。
<!-- DAILY_CHECKIN_2026-07-08_END -->

# 2026-07-07
<!-- DAILY_CHECKIN_2026-07-07_START -->


# Monad叙事

## 一、Monad 发展史

Monad 的故事始于 **2022 年 4 月**，做市巨头 Jump Trading 的研究负责人 Keone Hon 宣布离职，与两位前同事 James Hunsaker 和 Eunice Giarta 共同创立 Monad Labs。他们的核心判断是：EVM 已成为加密世界的“标配”，但以太坊主网性能严重不足，而 Layer 2 的分层策略又会破坏链上可组合性。因此，他们选择在 Layer 1 层面通过“并行处理”来改进 EVM 的执行效率。

**融资历程**贯穿了完整的市场周期：

| 轮次 | 时间 | 金额 | 领投方 |
| --- | --- | --- | --- |
| Pre-Seed | 2022年5月 | $900万 | 天使投资人、Jump Trading 网络 |
| Seed | 2023年2月 | $1,900万 | Dragonfly Capital |
| Series A | 2024年4月 | $2.25亿 | Paradigm（2024年最大加密融资） |
| 战略轮 | 2024年5月 | 未披露 | OKX Ventures 等 |
| IEO（公募） | 2025年11月 | $2.69亿 | Coinbase 平台，85,820 人参与 |

累计融资超过 **4.3 亿美元**。

**技术路线图**稳步推进：

-   **2023年9月**：官方文档发布
    
-   **2025年2月9日**：公共测试网上线，区块生成时间仅 **0.5 秒**
    
-   **2025年5月5日**：启动验证者专属 Testnet-2
    
-   **2025年11月24日**：**主网正式上线**
    

主网数据表现亮眼：总供应量 **1000 亿 MON**，向 **22.5 万名用户**进行空投。截至 2026 年 7 月，Monad TVL 已达到 **4.479 亿美元**，超越 Sui，在所有公链中位居第 **13** 位。

## 二、Monad 的优势与劣势

### ✅ 核心优势

**1\. 100% EVM 兼容（字节码级别）**

现有以太坊智能合约和生态工具可以直接迁移至 Monad，无需大幅改动。开发者可以使用 Solidity、MetaMask、Foundry 等熟悉的工具，无需学习新语言。“尽可能少动 EVM 规则”的设计哲学，使其成为“EVM 世界的并行加速器”。

**2\. 高性能与低费用**

目标性能为 **10,000 TPS**、**1 秒最终确认**、**低于 1 美分的平均 Gas 费**。Monad 通过四大技术创新实现这一目标：**乐观并行执行**（假设无冲突，同时运行所有交易，碰撞时再重新执行）、**MonadBFT 共识**（约 1 秒完成最终确认）、**异步执行**（共识与执行解耦）以及 **MonadDB**（专为高性能状态管理设计）。

**3\. 消费级硬件可运行节点**

相比 Solana 的数据中心级硬件要求，Monad 优化了节点性能，降低了硬件门槛。虽然目前仍推荐较高配置，但设计目标之一是让更多用户参与网络验证。

**4\. 顶级资本背书与生态冷启动能力**

超 **4.3 亿美元**融资储备，生态已聚集超 **280 个项目**，累计生态融资达 **13.2 亿美元**。Aave V3 上线 48 小时内存款突破 1 亿美元，Uniswap、Curve 等头部协议已部署。

### ❌ 核心劣势与挑战

**1\. 性能尚未被大规模验证**

主网上线仅半年多，**10,000 TPS 在复杂真实场景下的稳定性仍需时间验证**。市场对“性能叙事”的敏感度已明显降低——**高 TPS 早已不再是稀缺指标**。

**2\. 激励驱动的增长能否持续**

当前 TVL 和用户活跃度很大程度上由激励计划驱动。**激励退潮后能否留住用户和流动性**，是 Monad 面临的最大考验。

**3\. 代币经济学的长期压力**

MON 总供应量 **1000 亿枚**，团队（27%）和早期投资者（19.7%）持有大量代币。流通供应量仍远低于总供应量，**未来的解锁、验证者奖励、生态排放对价格表现至关重要**。MON 价格从公售价 $0.025 短暂冲高后有所回落。

**4\. 竞争格局极其激烈**

Monad 面临的不是单一对手，而是**以太坊 L2、Solana、Sui/Aptos（Move 生态）以及同赛道并行 EVM 项目（Sei、MegaETH）** 的多维度竞争。

## 三、与以太坊、Solana、Sui 的全面对比

### ⚙️ 执行模型：四者的根本差异

| 公链 | 执行模型 | 核心逻辑 |
| --- | --- | --- |
| 以太坊 | 顺序执行 | 交易像单车道上一辆接一辆处理 |
| Solana | 预先声明并行 | 交易需提前声明访问哪些账户，Sealevel 据此调度 |
| Sui | 以对象为中心（Object-Centric） | 将资产视为独立对象，拥有对象交易可绕过全局共识 |
| Monad | 乐观并行执行（Speculative EVM） | 默认无冲突，先并行执行；冲突时再按序重新执行 |

### 📊 全方位对比

| 维度 | 以太坊 | Solana | Sui | Monad |
| --- | --- | --- | --- | --- |
| 执行模型 | 顺序执行 | 预先声明并行 | 以对象为中心 | 乐观并行 EVM |
| TPS（峰值/目标） | ~15 | 数千（持续） | 亚秒级最终确认（拥有对象） | 10,000+ |
| 最终确认时间 | ~12-15 分钟 | ~400 毫秒 | <1 秒（拥有对象） | ~1 秒 |
| 虚拟机 (VM) | EVM（Solidity） | 非 EVM（Rust） | 非 EVM（Move） | 100% EVM 兼容 |
| 开发者语言 | Solidity | Rust | Move | Solidity（无需更改） |
| 验证者硬件 | 消费级 | 数据中心级 | 中等 | 数据中心级 |
| DeFi TVL（约） | 数千亿美元 | ~247.8 亿美元 | ~4.4 亿美元 | ~4.479 亿美元 |

### 🎯 各公链核心定位

**以太坊：“守成之王”**

拥有最大的开发者网络、最深的流动性和最强的网络效应。EVM 是加密领域最重要的开发者环境。但底层性能瓶颈明显，扩容依赖 L2。

**Solana：“性能之王”**

经过多轮牛熊考验的蓝筹高性能公链，在高吞吐和低费用场景中优势明显。但开发者需学习 Rust，节点硬件门槛高。

**Sui：“架构创新者”**

基于 Move 语言，以对象为中心的并行模型提供了独特优势。拥有对象交易可实现亚秒级最终确认。但生态仍在建设，Move 学习曲线对以太坊开发者不够友好。

**Monad：“EVM 的挑战者”**

核心定位可概括为 **“拥有 Solana 速度的以太坊”** 。100% EVM 兼容是最大王牌——以太坊开发者和现有应用可以**无缝迁移**，获得性能的巨大提升。Monad 选择了兼容路径，更像是 **“性能版以太坊”** ，是 EVM 世界的并行加速器。

## 💎 总结

Monad 试图在 **“性能、兼容性、去中心化”** 三者之间找到最优平衡点。它既不像 Solana 那样需要开发者学习新语言，也不像以太坊 L2 那样受限于主网的最终结算延迟。它提供了一个 **“熟悉但更快”** 的开发环境。

但挑战同样明确：**“更快”的叙事已经不够稀缺**。Monad 需要在激励退潮后证明自己拥有**不可替代的真实需求**——真实的 Gas 消耗、真实的 DeFi 活动、真实的用户留存。正如其空投后的表现所示：**空投驱动的关注度并不等同于产品与市场契合**。真正的考验，才刚刚开始。
<!-- DAILY_CHECKIN_2026-07-07_END -->

# 2026-07-06
<!-- DAILY_CHECKIN_2026-07-06_START -->



Monad 的理解：**一个从底层软件架构出发，旨在解决“去中心化”与“高性能”对立关系的高性能 Layer-1 区块链。它试图在完全兼容以太坊生态的前提下，解决主流区块链的性能瓶颈。**

**核心技术：**  
一、**MonadBFT 共识机制**：这是Monad的共识核心，是一个前沿的拜占庭容错（BFT）协议。

-   **高性能**：它能实现**单轮（400毫秒）的推测性最终确认**和**两轮（800毫秒）的完全最终确认**。
    
-   **大规模节点支持**：在正常（无故障）路径下，其消息复杂度是线性的，允许共识节点集扩展到很大的规模。
    
-   **关键特性**：它具备**乐观响应性**（网络有多快，共识就有多快）、**领导容错**（单个失败领导者只带来一次超时延迟）以及对**尾分叉（tail-forking）攻击的抵抗能力**。
    

**二、RaptorCast**：一种高效的区块传输协议，用于优化网络层的数据分发。

**三、异步执行（Asynchronous Execution）**：这是Monad架构中非常关键的一环。它将**共识（交易排序）** 和**执行（交易计算）** 解耦。

-   **传统模式的问题**：在以太坊等链上，执行是共识的前提，导致执行时间被严重压缩，仅占区块时间的很小一部分。
    
-   **Monad的方案**：节点先对交易的**顺序**达成共识，然后再去执行它们。这使得执行可以占用**整个区块时间**，极大地提高了执行预算。
    

**四、并行执行（Parallel Execution）**：Monad 采用**乐观执行**策略。

-   **工作原理**：它会**假设交易之间没有冲突，同时开始执行一个区块内的所有交易**。
    
-   **冲突处理**：如果发生冲突（如两笔交易操作了同一个账户），系统会**重新执行**受影响的交易。由于输入数据已在缓存中，重新执行的速度远快于首次执行。最终，Monad 的执行结果与以太坊**完全相同**。
    

**五、MonadDb**：专为以太坊状态存储设计的定制化**键值数据库**。

-   **原生优化**：它原生实现了以太坊使用的**默克尔帕特里夏树（Merkle Patricia Trie）** 数据结构，避免了“一种数据结构嵌入另一种”的低效问题。
    
-   **异步I/O**：充分利用了 Linux 最新的 `io_uring` 异步I/O接口，避免因I/O操作阻塞执行。
    
-   **极致性能**：为追求极致性能，MonadDb 甚至可以**绕过文件系统，直接在块设备上运行**，以完全解锁SSD的性能潜力。
<!-- DAILY_CHECKIN_2026-07-06_END -->
<!-- Content_END -->
